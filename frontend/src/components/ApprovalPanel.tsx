import { useState } from "react";
import { ShieldAlert } from "lucide-react";
import { Panel } from "./Panel";
export function ApprovalPanel({ filename = "sustainability-report.md", onApprove, onReject, onEdit, message }: { filename?: string; onApprove?: () => void; onReject?: () => void; onEdit?: (filename: string) => void; message?: string }) { const [currentFilename, setCurrentFilename] = useState(filename); return <Panel title="Human approval required" icon={<ShieldAlert size={17} />}><div className="approval-panel"><strong>Save sustainability report</strong><span>Risk level: MEDIUM</span><input value={currentFilename} aria-label="Report filename" onChange={(event) => setCurrentFilename(event.target.value)} /><div><button type="button" className="approval-reject" onClick={onReject}>Reject</button><button type="button" className="approval-edit" onClick={() => onEdit?.(currentFilename)}>Edit & approve</button><button type="button" className="ask-button" onClick={onApprove}>Approve</button></div>{message && <small className="approval-message">{message}</small>}</div></Panel>; }
